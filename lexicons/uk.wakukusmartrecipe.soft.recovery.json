{
  "lexicon": 1,
  "id": "uk.wakukusmartrecipe.soft.recovery",
  "defs": {
    "main": {
      "type": "record",
      "key": "literal:self",
      "record": {
        "type": "object",
        "required": ["version", "alg", "iv", "wrappedKey", "updatedAt"],
        "properties": {
          "version": {"type": "integer", "minimum": 1, "maximum": 1},
          "alg": {"type": "string", "knownValues": ["A256GCM"]},
          "iv": {"type": "string", "maxLength": 32},
          "wrappedKey": {"type": "string", "maxLength": 128},
          "updatedAt": {"type": "string", "format": "datetime"}
        }
      }
    }
  }
}
